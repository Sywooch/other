String.prototype.toDate = function(separator) {    separator = separator || "."    var el = this.split(separator);    if (el.length != 3) return null;    var day = parseInt(el[0][0] == '0' ? el[0][1] : el[0]);    var month = parseInt(el[1], 10) - 1;    var year = parseInt(el[2])    year += (year < 100 ) ? ((year <= 20) ? 2000 : 1900) : 0;    if (month < 0 || month > 11) return null;    d = new Date(year, month + 1, 0);    ld = d.getDate();    if ( day < 1 || day > ld) return null;    return new Date(year, month, day);}